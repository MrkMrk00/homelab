apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dnsmasq
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/MrkMrk00/homelab.git
    targetRevision: master
    path: kubernetes/dnsmasq
  destination:
    server: https://kubernetes.default.svc
    namespace: dnsmasq
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
