- name: Enable Debian trixie-backports
  ansible.builtin.apt_repository:
    filename: "trixie-backports"
    repo: "deb http://deb.debian.org/debian trixie-backports main contrib non-free non-free-firmware"
    state: present
    update_cache: true

- name: Pin trixie-backports repository to low priority
  ansible.builtin.copy:
    dest: /etc/apt/preferences.d/99-backports
    mode: "0644"
    content: |
      Package: *
      Pin: release a=trixie-backports
      Pin-Priority: 100

- name: Install backported kernel (trixie-backports/linux-image-amd64)
  ansible.builtin.apt:
    name:
      - linux-image-amd64
      - linux-headers-amd64
    state: present
    default_release: "trixie-backports"
  register: kernel_install


- name: Install firmware packages
  ansible.builtin.apt:
    name:
      - firmware-amd-graphics
      - firmware-misc-nonfree
      - amd64-microcode
    state: present

- name: Ensure required kernel modules are loaded at boot
  ansible.builtin.copy:
    dest: /etc/modules-load.d/k3s.conf
    owner: root
    group: root
    mode: "0644"
    content: |
      br_netfilter
      overlay

- name: Load br_netfilter module
  ansible.builtin.command: modprobe br_netfilter
  changed_when: false

- name: Load overlay module
  ansible.builtin.command: modprobe overlay
  changed_when: false

- name: Configure kernel parameters for Kubernetes (k3s)
  ansible.builtin.copy:
    dest: /etc/sysctl.d/99-k3s.conf
    owner: root
    group: root
    mode: "0644"
    content: |
      # --- Kubernetes required networking ---
      net.ipv4.ip_forward = 1
      net.ipv6.conf.all.forwarding = 1
      net.bridge.bridge-nf-call-iptables = 1
      net.bridge.bridge-nf-call-ip6tables = 1

      # --- Memory behavior ---
      vm.swappiness = 0

      # --- File system watchers (kubelet, containerd, CSI) ---
      fs.inotify.max_user_instances = 8192
      fs.inotify.max_user_watches = 524288

      # --- Network scalability ---
      net.core.somaxconn = 32768
      net.ipv4.tcp_max_syn_backlog = 8192

      # --- TCP connection stability ---
      net.ipv4.tcp_keepalive_time = 600
      net.ipv4.tcp_keepalive_intvl = 30
      net.ipv4.tcp_keepalive_probes = 10

      # --- Conntrack (pods/services NAT) ---
      net.netfilter.nf_conntrack_max = 262144

- name: Apply sysctl settings
  ansible.builtin.command: sysctl --system
  changed_when: false

- name: Reboot into new kernel (if changed)
  ansible.builtin.reboot:
    reboot_timeout: 600
  when: kernel_install.changed
